meta {
  name: Metrics - trigger request
  type: http
  seq: 2
}

post {
  url: {{base_url}}/v1/chat/completions
  body: json
  auth: inherit
}

body:json {
  {
    "model": "local",
    "messages": [{"role": "user", "content": "hello world"}]
  }
}

tests {
  test("status is 200", function() {
    expect(res.status).to.equal(200);
  });
}
